<header {{ attributes }}>
    {% do action('jankx_component_before_header_content') %}

    {{ open_container }}

        {% do action('jankx_component_before_header') %}
        {{ content }}
        {% do action('jankx_component_after_header') %}

    {{ close_container }}

    {% do action('jankx_component_after_header_content') %}
</header>
